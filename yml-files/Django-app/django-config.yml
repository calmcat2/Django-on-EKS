apiVersion: v1
kind: ConfigMap
metadata:
  name: app-cm
  namespace: django-app
data:
  DB_PORT: "3306"
  DB_HOST: "mysql-svc"
  STATIC_ROOT: "/data/static"
  DEBUG: "False"
  ALLOWED_HOSTS: "localhost,app2.maxinehe.top"
  CSRF_TRUSTED_ORIGINS: "https://app2.maxinehe.top"
  MYSQL_DATABASE: "littlelemon"
 
